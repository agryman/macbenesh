/*	name:	:misc:graphics.h	type:	C++		This file defines macros for operations on Points, Rects, and other QuickDraw datatypes.Programmers:DM	Doug MoenAGR	Arthur RymanChange History:AGR	V100	1991/11/22	Upgraded to C++.AGR	V101	1993/12/04	Guard against repeated inclusionAGR	V102	1995/12/31	Use inline functions instead of macros.*/#ifndef MISC_GRAPHICS_H#define MISC_GRAPHICS_Hinline short Height(const Rect* r) {return r->bottom - r->top;}inline short Width(const Rect* r) {return r->right - r->left;}inline short CenterV(const Rect* r) {return (r->top + r->bottom) / 2;}inline short CenterH(const Rect* r) {return (r->left + r->right) / 2;}inline int SCenterV(const Rect* r, int scale) {return (r->top + r->bottom) * scale / 2;}inline int SCenterH(const Rect* r, int scale) {return (r->left + r->right) * scale / 2;}inline Point& TopLeft(Rect& r) {return *(Point*) &r.top;}inline Point& BotRight(Rect& r) {return *(Point*) &r.bottom;}inline void MultPt(Point& pt, short m) {pt.h *= m; pt.v *= m;}inline void DivPt(Point& pt, short d) {pt.h /= d; pt.v /= d;}#endif