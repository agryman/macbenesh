/*	name:	:misc:list.c	type:	C++		This is the implementation of the List, ListElement, and ListIterator classes.A List represents a sequence of void* values.Programmers:AGR	Arthur RymanChange History:AGR	V100	1995/12/21	PTR 7. Created.*/#include ":src:misc:list.h"#pragma template_access publictemplate<class T> List<T>::~List(){	while(first)	{		ListElement<T>* element = first;		first = first->next;		delete element;	}}template<class T> void List<T>::Append(T value){	ListElement<T>* append = new ListElement<T>(value);		if(first)	{		ListElement<T>* last = first;		while(last->next)			last = last->next;		last->next = append;	}	else		first = append;}template<class T> void List<T>::Remove(T value)	// remove first element that contains value{	ListElement<T>* remove = first;	ListElement<T>* before = 0;		while(remove && remove->val != value)	{		before = remove;		remove = remove->next;	}	if(remove)	{		if(remove == first)			first = first->next;		else			before->next = remove->next;				delete remove;	}}template<class T> ListIterator<T>::ListIterator(const List<T>& list) :	current(list.first){}	template<class T> void ListIterator<T>::Next(){	// assert(current != 0);		if(current)		current = current->next;}template<class T> T ListIterator<T>::Value(){	// assert(current != 0);		return current ? current->val : 0;}