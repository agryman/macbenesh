/* * Version 2 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * kitsAndScore.c * * Change History: * 1991/11/22	AGR	V100	Upgraded to C++. * AGR	V101	1992/12/28	Added GetFrontmostScoreWindow.AGR	V102	1994/11/27	Minimize includes.AGR	V103	1995/12/31	Use OOP for Score.AGR	V104	1997/01/02	Do not change relative position of clip window.*/#ifndef HEADER_H#include ":src:header.h"		// WindowPtr#endif#ifndef MAIN_UTIL_H#include ":src:main:util.h"		// TRUE, FALSE#endif#ifndef MAIN_WINDOWS_H#include ":src:main:windows.h"	// scoreKind#endif#ifndef MISC_MISPROTO_H#include ":src:misc:misproto.h"	// GetFrontmostWindowKind#endif#ifndef MISC_SCORES_H#include ":src:misc:scores.h"	// Score#endif// Return the WindowPtr of the frontmost window of the given kind, or 0 if no such window is open.WindowPtr GetFrontmostWindowKind(int aKind)	// V101{	for(WindowPeek w = windowList; w; w = w->nextWindow)		if(w->windowKind == aKind)			break;		return (WindowPtr) w;}/* * Bring all of the kit windows to the front, * and either the frontmost (if scoreWindow == 0) or the specified * score window just behind them. */void KitsAndScoreToFront(WindowPeek scoreWindow){	WindowPeek w, lastKit;	w = windowList;	if (w == 0)		return;	/* Bring all the kit windows to the front. */	lastKit = (WindowPeek)(-1);	while (w != 0) {		WindowPeek next = w->nextWindow;		if (w->windowKind == kitKind) {			BringForward(w, lastKit);			lastKit = w;		}		w = next;	}	/*	 * At this point, lastKit is the rear-most kit window, or -1.	 * Bring the score window up behind lastKit.	 */	if (scoreWindow == 0) {		w = windowList;		while (w != 0 && w->windowKind != scoreKind)			w = w->nextWindow;		scoreWindow = w;	}	if (scoreWindow != 0)		BringForward(scoreWindow, lastKit);		/*	 * Go through the window list and make sure every window is	 * correctly highlighted.	 */	for (w = windowList; w != 0; w = w->nextWindow) {		if (w->windowKind == kitKind || w == scoreWindow)			HiliteWindow((WindowPtr)w, TRUE);		else			HiliteWindow((WindowPtr)w, FALSE);	}}/* * Bring 'window' forward, so that it is behind 'behind', * or frontmost if 'behind' == -1. */void BringForward(WindowPeek window, WindowPeek behind){	if (behind == (WindowPeek)(-1)) {		if (windowList != window)			BringToFront((WindowPtr)window);	} else {		if (behind->nextWindow != window) {			SendBehind((WindowPtr)window, (WindowPtr)behind);			PaintOne(window, window->strucRgn);		}		CalcVis(window);	}}