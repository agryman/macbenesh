/* * Version 3 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * mlines.h * Movement line data structures and definitions. * * Change History: * 1991/11/23	AGR	V100	Upgraded to C++.AGR	V101	1994/11/27	Replace struct by class. */#ifndef MLINE_MLINES_H#define MLINE_MLINES_H#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// Sign#endifclass Stave;/* * An arbitrary point within the score, * in stave coordinates, plus the stave. */class StavePoint{public:	Stave **stave;	Point pt;};/* * The endpoints of an mline. */class EndPoints{public:	StavePoint start, end;};class Frame;/* * This is the master record describing a movement line. */class MLine{public:	Sign sign;	EndPoints endpoints;	Frame **startFrame, **endFrame;};/* * One of these is stored in each stave that a movement line intersects. */class MSegment {public:	Point start, end;	MLine **mline;	MSegment **next;};/* * Movement line endpoints are stored in the frames in which they occur * as symbols.  See symbols.h *//* * Endpoint of a movement line. * Used by DragLine(). */class MPoint{public:	Point pos;	/* stave coordinates */	short topStave;	Stave **stave;};/* values returned by FindLine() */#define L_OUTSIDE	0#define L_START		1#define L_MIDDLE	2#define L_END		3#endif