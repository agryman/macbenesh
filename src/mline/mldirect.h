/* * Version 3 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * mldirect.h * * Change History: * 1991/11/23	AGR	V100	Upgraded to C++.AGR	V101	1994/11/16	Remove global variable defintions.*/#ifndef MLINE_MLDIRECT_H#define MLINE_MLDIRECT_H/* * A direction is an integer from 0 to 7 representing one of the * 8 compass directions: N=0, NE=1, E=2, ... NW=7 */typedef char Direction;extern Direction HDelta[8];	// V101extern Direction VDelta[8];	// V101#define DeltaH(dir)	HDelta[dir]#define DeltaV(dir) VDelta[dir]/* * Two directions can be added together. * What happens is that one of the directions actually specifies an * angle (measured in 8'ths of a circle) that the other is offset by. */#define AddDir(d1, d2) (((d1) + (d2) + 32) % 8)#define SubDir(d1, d2) (((d1) - (d2) + 32) % 8)/* * A Vector encodes an arbitrary direction on the I x I plane. */typedef Point Vector;/* * An Angle specifies a range of Directions, * starting at 'base' and extending 'offset' eighths of a circle * in the clockwise direction if offset is positive, * or the counterclockwise direction if offset is negative. */typedef struct {	Direction base, offset;	/*	 * blatantFiller is to work around a compiler bug.	 * Its purpose is to pad the structure out to 4 bytes.	 */	short blatantFiller;} Angle;#define AngleSize(a) abs((a).offset)#endif