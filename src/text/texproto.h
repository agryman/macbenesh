/*	name:	:text:texproto.h	type:	MPW C++		This file contains the function prototypes for Text.	Programmers:AGR	Arthur RymanChange History:AGR	V100	1991/11/30	Created.AGR	V101	1993/11/18	Split font.c.AGR	V102	1993/12/12	Guard against repeated inclusion.AGR	V103	1995/12/21	PTR 45. Remove dead code.AGR	V104	1995/12/21	PTR 7.AGR	V105	1995/12/31	Use OOP for Score.AGR	V106	1997/03/14	Eliminate globals.AGR	V107	1997/09/14	Fix DCR 107 - Reduce clipping PICT size.*/#ifndef TEXT_TEXPROTO_H#define TEXT_TEXPROTO_H#ifndef MISC_LIST_H#include ":src:misc:list.h"	// List V104#endif#ifndef PS_PS_H#include ":src:ps:ps.h"			// PSSpace V107#endifclass Frame;			// V102class FrameContext;		// V102class Score;			// V102class Sign;				// V102class Stave;			// V102class Symbol;			// V102class TextLine;			// V102class TextLocation;		// V102class TextRecord;		// V102class List<TextRecord**>;	// V104class TSegment;			// V102// document.cvoid CheckAlign(short alignID);void SetDestRect(Rect *destRect, TextRecord **tR, short align, int scale, Score* score);	// V106// font.cvoid UpdateFont(TextRecord **textRecord);class CheckFontsInterface {public:	enum {		FONT,		SIZE,		FACE	};};void CheckFonts(short what, short ID, Score* score);				// V106void AppendTextRecords(List<TextRecord**>& list, Symbol** symbols);	// V104void CheckFont(short fontID);void CheckSize(short sizeID);void CheckFace(short faceID);void ClearTextCmd(void);void SetLineHeight(short spacing, TERec **teRec);void SetFontAttributes(TextRecord **textRecord);void SetCurrentFont(TextRecord **textRecord);void SetToCurrentFont(void);void ChangeAllCmd(void); // text.cvoid TextCmd(void);void OpenTextRecord(TextRecord **textRecord, Score* score);							// V106void OpenTERec(Rect *destRect, Rect *viewRect, short align, WindowPtr viewWindow);	// V106int CloseTextRecord(void);void CloseTERec(void);TextRecord **NewTextRecord(void);void EraseTextRecord(TextRecord **textRecord, Score* score);						// V106void EraseTextSymbol(TextRecord **textRecord, Score* score);						// V106void EraseTextSymbolFromList(TextRecord **textRecord, Symbol **symbolList,	Frame **frame, Score* score);													// V106void FreeTextRecord(TextRecord **textRecord);TextRecord **DupTextRecord(TextRecord **original);Sign **TextRecordToSign(TextRecord **textRecord);int WriteFreeText(TextRecord **textRecord, int file);int ReadFreeText(Symbol **symbol, Frame **frame, int file);void TextRect(WindowPtr w, Rect *r);int MoveText(int select, int shift, int modifier, int mode, Score* score,	FrameContext *f, Point pickPt, Point scorePt);// textDraw.c// textEdit.cvoid ChangeTextInsertion(EventRecord *event);void UpdateTextRecord(char c);void UpdateTERec (Score* score);													// V106void ScaleTERec(int oldScale, int newScale);										// V106int WhichFramePartText(TextRecord **textRecord);// textFormat.cvoid SetTextLocation(TextLocation **textLoc, Point anchorSpot, Score* score);TextLocation **NewTextLocation(void);int ParseTextRecord(TextRecord **textRecord, short textLength, Handle theText,	TextLocation **curTextLoc, short align, short numLines, Score* score);			// V106Handle CopyText(Handle srcText, int length, int reversed);short FreeTextWidth(Handle theText, short length);// textLine.cTextLine **NewTextLine(void);void FreeTextLine(TextLine **textLine);TERec **SetParseTERec(TextRecord **tR, Stave **curStave, short align,	Handle theText, short length);int PtToStaveEnd(Stave **stave, Point pt);// textSegments.cTSegment **NewTSegment(void);void FreeTSegments(TSegment **tSegment);void FreeTSegment(TSegment **tSegment);void DrawTSegment (PSSpace psState, TSegment **tSegment, int scale, Stave **stave);	// V107void AddTSegmentToStave(TSegment **tSegment, Stave **stave);void RemoveTSegments(TextRecord **textRecord, Score* score, int redraw);void RemoveTSegment(Stave **stave, TextRecord **textRecord, Score* score,	int redraw);void InstallTSegments(TextRecord **textRecord, Score* score, int redraw);void CalcTextDrawBox(Rect *destRect, Stave **stave, TSegment **tSegment,	int scale);void SetNumberTextLines(short *numLines, Handle theText, short length);#endif