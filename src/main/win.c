/*	name:	:main:win.c	type:	C++		This is the definition of the Window class.	Programmers:AGR	Arthur RymanChange History:AGR	V100	1996/04/05	Created.AGR	V101	1997/01/03	Add parent.AGR	V102	1997/03/14	Add getWTitle ().AGR	V103	1997/05/03	Add invalidateWindow ().*/#ifndef MAIN_WINDOW_H#include ":src:main:win.h"	// Window#endifWindow::Window(Target* aParent) {	parent = aParent;	// V101	windowPtr = 0;		// V102}Window::~Window() {}void Window::DoIdle() {}void Window::getWTitle (Str255 title) {	// V102	WindowPtr w = getWindowPtr ();	if (w) {		::GetWTitle (w, title);	}}/** * Invalidate the entire window. */void Window::invalidateWindow () {	// V103	WindowPtr w = getWindowPtr ();	if (w != 0) {		GrafPtr savePort;		::GetPort (&savePort);		::SetPort (w);		::InvalRect (&w->portRect);		::SetPort (savePort);	}}