/*	name:	:main:pictpars.h	type:	C++		This is the definition of the PICTParser class, which parses a PictHandle into operations.Programmers:AGR	Arthur RymanChange History:AGR	V100	1997/06/29	Created to fix DCR 106 - round trip clippings.*/#ifndef MAIN_PICTPARS_H#define MAIN_PICTPARS_H/** * A picture has a version followed by a sequence of operations. * Versions and operations are elements. We are concerned with VersionOne version * and ShortComment and LongComment operations. */ /** * A PICTElement is an abstract PICT element. * Each element is located at some offset within the handle * and get a size. Each element can be checked for correct syntax. */ class PICTElement {private:	PicHandle pic;	unsigned short offset;	protected:	char getChar (unsigned short i);	unsigned char getUnsignedChar (unsigned short i);		void getChars (unsigned short i, unsigned short length, char* buffer);	void getUnsignedChars (unsigned short i, unsigned short length, unsigned char* buffer);		short getShort (unsigned short i);	unsigned short getUnsignedShort (unsigned short i);		long getLong (unsigned short i);	unsigned long getUnsignedLong (unsigned short i);	public:	PICTElement (PicHandle aPic, unsigned short aOffset);	virtual ~PICTElement ();	virtual Boolean isOK () = 0;	virtual unsigned short getSize () = 0;};/** * A PICTVersion is an abstract PICT version element. */class PICTVersion : public PICTElement {public:	PICTVersion (PicHandle aPic, unsigned short aOffset);	virtual ~PICTVersion ();};/** * A PICTVersionOne is a concrete PICT version. * We are only concerned with version one. */class PICTVersionOne : public PICTVersion {public:	PICTVersionOne (PicHandle aPic, unsigned short offset);	virtual ~PICTVersionOne ();		virtual Boolean isOK ();	virtual unsigned short getSize ();		short getVersionOne ();		static const short KEY_VERSION_ONE;protected:	unsigned short versionOne_OFFSET;	unsigned short versionOne_SIZE;};/** * A PICTOperation is an abstract PICT operation element. * Each operation has an opcode. */class PICTOperation : public PICTElement {public:	PICTOperation (PicHandle aPic, unsigned short aOffset);	virtual ~PICTOperation ();	char getOpcode ();protected:	unsigned short opcode_OFFSET;	unsigned short opcode_SIZE;};/** * A PICTComment is an abstract PICT comment operation. * Each comment has a kind. */class PICTComment : public PICTOperation {public:	PICTComment (PicHandle aPic, unsigned short aOffset);	virtual ~PICTComment ();	short getKind ();protected:	unsigned short kind_OFFSET;	unsigned short kind_SIZE;};/** * A PICTShortComment is a concrete comment. * It contains no additional data. */class PICTShortComment : public PICTComment {public:	PICTShortComment (PicHandle aPic, unsigned short aOffset);	virtual ~PICTShortComment ();	virtual Boolean isOK ();	virtual unsigned short getSize ();	static const char OPCODE_SHORT_COMMENT;	static const short KIND_PS_BEGIN;	static const short KIND_PS_END;};/** * A PICTLongComment is a concrete comment. * It contains an array of bytes. */class PICTLongComment : public PICTComment {public:	PICTLongComment (PicHandle aPic, unsigned short aOffset);	virtual ~PICTLongComment ();	virtual Boolean isOK ();	virtual unsigned short getSize ();	short getCommentSize ();					// number of bytes in the comment	char getCommentAt (unsigned short i);		// comment byte at index i	unsigned short getComments (unsigned short i,		unsigned short length, char* buffer);	// copy up to length bytes starting at i into buffer, return actual	static const char OPCODE_LONG_COMMENT;	static const short KIND_PS_HANDLE;protected:	unsigned short commentSize_OFFSET;	unsigned short commentSize_SIZE;	unsigned short commentData_OFFSET;	unsigned short commentData_SIZE;};/** * A PICTParser parses a PICT. * It maintains an offset into the PICT that advances when an * element is successfully parsed. */class PICTParser {private:	PicHandle pic;			// the picture	unsigned short offset;	// current offset into picturepublic:	PICTParser (PicHandle aPic);	Boolean isOK ();	unsigned short getSize ();	Rect getBounds ();		PICTElement* parseElement (PICTElement* element);	PICTVersionOne* parseVersionOne ();	PICTShortComment* parseShortComment ();	PICTLongComment* parseLongComment ();};#endif