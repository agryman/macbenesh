/* * Version 3 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * bracket.c * * Change History: * 1991/11/27 AGR V100	Upgraded to C++.AGR	V100	1994/11/23	Minimize includes.*/#ifndef HEADER_H#include ":src:header.h"		// Point#endif#ifndef SIGN_SIGPROTO_H#include ":src:sign:sigproto.h"	// DrawBracket#endif#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// brackLeft#endif/* * Draw a bracket sign. */void DrawBracket(short which, Point pos, int scale){	PenSize(scale, scale);	if (which == brackLeft) {		MoveTo(pos.h, pos.v);		Line(0, -6);		MoveTo(pos.h, pos.v - 3 * scale);		Line(18, 0);	} else {		MoveTo(pos.h, pos.v);		Line(0, -6);		MoveTo(pos.h, pos.v - 3 * scale);		Line(-18, 0);	}	PenSize(1, 1);}