/* * Version 3 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * bar.c *	Bar lines. * * Change History: * 1991/11/27 AGR V100	Upgraded to C++.AGR	V403	1994/11/23	Minimize includes.AGR	V404	1995/08/06	Conditionally compile debugging code.*/#ifndef HEADER_H#include ":src:header.h"		// PenSize#endif#ifndef MAIN_MAIPROTO_H#include ":src:main:maiproto.h"	// Message#endif#ifndef MISC_FORMAT_H#include ":src:misc:format.h"	// staveHeight#endif#ifndef SIGN_SIGPROTO_H#include ":src:sign:sigproto.h"	// BarWidth#endif#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// singleBar#endifint BarWidth(int bar){	switch (bar) {	case singleBar:	case dashedBar:		return 1;	case doubleBar:		return 4;	case thickDoubleBar:		return 5;	default:// V404		Message2("BarWidth: bad bar type %d", bar);		return 0;	}}void DrawBar(int barH, int barV, int scale){	MoveTo(barH, barV);	Line(0, (staveHeight - 1) * scale);}void DrawBarLine(int bar, int barH, int barV, int scale){	int i;	PenSize(scale, scale);	switch (bar) {	case singleBar:		DrawBar(barH, barV, scale);		break;	case dashedBar:		MoveTo(barH, barV);		for (i = 1; i <= 20; ++i) {			Line(0, 0);			Move(0, 2 * scale);		}		Line(0, 0);		break;	case doubleBar:		DrawBar(barH, barV, scale);		DrawBar(barH + 3 * scale, barV, scale);		break;	case thickDoubleBar:		DrawBar(barH, barV, scale);		DrawBar(barH + 3 * scale, barV, scale);		DrawBar(barH + 4 * scale, barV, scale);		break;	}}