/*	name:	:app:appframe.c	type:	C++		This is the implementation of the Application class.	Programmers:AGR	Arthur RymanChange History:AGR	V200	1996/12/29	Created.AGR	V201	1997/01/22	Use Menu*.*/#ifndef APP_APP_H#include ":src:app:app.h"			// Application#endif#ifndef MAIN_WINDOWS_H#include ":src:main:windows.h"		// scoreKind#endif#ifndef MENU_FRAMMENU_H#include ":src:menu:frammenu.h"		// FramesMenu#endif#ifndef MISC_SCORES_H#include ":src:misc:scores.h"		// Score#endifvoid Application::AdjustFramesMenu(WindowPeek w)	// V201{	// Enable the items if and only if the target window is a score.	int kind = w ? w->windowKind : 0;		Boolean bScore = kind == scoreKind;	framesMenu->ableItem (FramesMenu::Add, bScore);	framesMenu->ableItem (FramesMenu::Duplicate, FALSE);	framesMenu->ableItem (FramesMenu::Erase, bScore);	framesMenu->ableItem (FramesMenu::SelectAll, bScore);	framesMenu->ableItem (FramesMenu::Format, bScore);	framesMenu->ableItem (FramesMenu::Unformat, bScore);	framesMenu->ableItem (FramesMenu::NoPadding, bScore);	framesMenu->ableItem (FramesMenu::RePad, bScore);}Boolean Application::DoFramesMenu (int item) {	// V??	Boolean bQuit = FALSE;		Target* target = getTargetScore ();	if (target) {		switch (item) {		case FramesMenu::Add:			bQuit = target->DoAdd ();			break;					case FramesMenu::Duplicate:			break;					case FramesMenu::Erase:			bQuit = target->DoErase ();			break;					case FramesMenu::SelectAll:			bQuit = target->DoSelectAll ();			break;					case FramesMenu::Format:			bQuit = target->DoFormat ();			break;					case FramesMenu::Unformat:			bQuit = target->DoUnformat ();			break;					case FramesMenu::NoPadding:			bQuit = target->DoNoPadding ();			break;					case FramesMenu::RePad:			bQuit = target->DoRePad ();			break;		}	}			return bQuit;}