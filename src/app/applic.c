/*	name:	applic.c	type:	C++		This file implements checkLicence and related functions.	Programmers:AGR	Arthur RymanChange History:AGR	V100	1997/03/23	Created.*/#ifndef APP_APP_H#include ":src:app:app.h"		// Application#endif#ifndef APP_LICENCE_H#include ":src:app:licence.h"	// LicenceInfo#endif/** * Check the licence info and if not valid, open the About window * and request the licence info. We let the user proceed even if the * licence information is invalid. The user is encourage to licence the * product to avoid being prompted when ever the application is opened. */void Application::checkLicence () {	Boolean bValid = licenceInfo->isValid ();	if (!bValid) {		DoAboutMacBenesh ();		licenceInfo->requestLicenceInfo ();	}}