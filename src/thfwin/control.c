/*	name:	thfwin/control.c	type:	C++		This file implements some utility functions that act on controls.Programmers:AGR	Arthur RymanChange History:AGR	V100	1993/03/16	Created.AGR	V101	1993/03/17	Add ShowControl calls.AGR V102	1993/07/01	Don't alter control visibility. Only set quantities if necessary.AGR	V103	1993/12/03	Explicitly include required headers.*/#ifndef HEADER_H#include ":src:header.h"#endif#ifndef THFWIN_CONTROL_H#include ":src:thfwin:control.h"#endif// Adjust a control's frame.void AdjustControlFrame(ControlHandle hControl, Rect frame){	(*hControl)->contrlVis = kControlInvisible;	MoveControl(hControl, frame.left, frame.top);	(*hControl)->contrlVis = kControlVisible;	SizeControl(hControl, frame.right - frame.left, frame.bottom - frame.top);//	ShowControl(hControl);	// V101}// Adjust a control's minimum, maximum, and value.void AdjustControlMinMaxValue(ControlHandle hControl, int minimum, int maximum, int value){// V102	(*hControl)->contrlVis = kControlInvisible;	if(minimum != GetCtlMin(hControl))	// V102		SetCtlMin(hControl, minimum);			if(maximum != GetCtlMax(hControl))	// V102		SetCtlMax(hControl, maximum);		// V102	(*hControl)->contrlVis = kControlVisible;	if(value != GetCtlValue(hControl))	// V102		SetCtlValue(hControl, value);		//	ShowControl(hControl);	// V101}