/*	name:	thfwin/clikloop.c	type:	C++		This file implements functions called by TEClikLoop.Programmers:AGR	Arthur RymanChange History:AGR	V100	1993/03/16	Cloned from TESample.AGR	V101	1993/11/23	Use EndTEScroll to invalidate exposed region of window and update window.AGR	V102	1993/12/03	Explicitly include required header files.AGR	V103	1993/12/10	Use TRegion.AGR	V104	1993/12/19	Preserve the text attributes in the clik loop.AGR	V105	1995/08/03	Port to Universal Headers. Replace ClikLoopProcPtr by TEClickLoopUPP.*/#ifndef HEADER_H#include ":src:header.h"#endif#ifndef AUDIT_H#include ":src:audit.h"#endif#ifndef THFWIN_TEXTATTR_H#include ":src:thfwin:textattr.h"	// V104 TTextAttribute#endif#ifndef THFWIN_THFWIN_H#include ":src:thfwin:thfwin.h"#endif#ifndef THFWIN_CLIKLOOP_H#include ":src:thfwin:clikloop.h"#endif#ifndef TOOLBOX_REGION_H#include ":src:toolbox:region.h"#endif// Gets called from our assembly language routine, AsmClikLoop, which is in// turn called by the TEClick toolbox routine. Saves the windows clip region,// sets it to the portRect, adjusts the scrollbar values to match the TE scroll// amount, then restores the clip region.pascal void PascalClikLoop(void){	auditEnter("PascalClikLoop");		TRegion region;						// V103	region.GetClip();					// save clip 		WindowPtr wind = FrontWindow();	audit2("FrontWindow = ", wind);		SetPort(wind);						// V104 Save the text attributes.	TTextAttribute attr;	attr.GetPortTextAttributes();		ClipRect(&wind->portRect);		THFWindow *doc = (THFWindow *) GetWRefCon(wind);	audit2("THFWindow * = ", doc);		doc->EndTEScroll();					// V101	doc->DoUpdate();					// V101	doc->AdjustScrollBarSettings();		region.SetClip();					// restore clip 		SetPort(wind);						// V104 Restore the text attributes.	attr.SetPortTextAttributes();			auditExit();}// Gets called from our assembly language routine, AsmClikLoop, which is in// turn called by the TEClick toolbox routine. It returns the address of the// default clikLoop routine that was put into the TERec by TEAutoView to// AsmClikLoop so that it can call it.pascal TEClickLoopUPP GetOldClikLoop(void)	// V105{	auditEnter("GetOldClikLoop");		THFWindow *doc = (THFWindow *) GetWRefCon(FrontWindow());		TEClickLoopUPP oldClikLoop = doc ? doc->GetClikLoop() : 0;	// V105	audit2("oldClikLoop = ", oldClikLoop);		auditExit();		return oldClikLoop;}