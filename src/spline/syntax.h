/*								syntax.h       This file contains a miscellaneous collection of macros     whose purpose is to translate my idea of civilized programming     language syntax into C syntax.  My apologies to C hacks, but    in this respect at least, C sucks.    Programmed by: jcbeattyChange History:1991/12/01 AGR V100	Upgraded to C++.*/#ifndef SPLINE_SYNTAX_H#define SPLINE_SYNTAX_H#define	BEGIN		{#define END			}#define WHILE		while (#define DO			) {#define E_WHILE		}#define REPEAT		do {#define UNTIL		} while (!(#define E_REPEAT	)) #define LOOP		for(;;){#define E_LOOP		}#define SWITCH		switch(#define E_SWITCH	}#define IF			if(#define THEN		){#define ELSE		}else{#define ELIF		}else if(#define FI			}#define E_IF		}#define FOR		for(#define E_FOR		}#endif