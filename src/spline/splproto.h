/*	name:	splproto.h	type:	MPW C++		This file contains the function prototypes for $DIYSplines.Change History:1991/11/30 AGR V100	Created.*/#ifndef SPLINE_SPLPROTO_H#define SPLINE_SPLPROTO_Hclass Score;class Sign;class Spline;// BsplBValues.cvoid BsplBValues(float knots[], int k, float u, int delta, float basisFcns[]);// BSplineBasesForFit.cfloat **BSplineBasesForFit(float bases[], float uInd[], int ncv, int ndata,	Boolean usw);// Decomp.cvoid Decomp(float a[], float *cond, int   ipvt[], int   ndim);// Fit.cBoolean MinCV(int m, int ncv);void Fit(Point **points, int nData, Spline **curve);// GetStroke.cPoint **GetStroke(int *nData);// Residuals.cfloat Residuals(int typeCurve, float *uInd, Point *data, float *xcoef,	float *ycoef, float *knots, float *bases, int ndata, int ncv);// Solve.cvoid Solve(float a[], float b[], int ipvt[], int ndim);// trackSCurve.cSpline **NewSCurve(void);#endif