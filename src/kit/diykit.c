/*	name:	:kit:diykit.c	type:	C++		This is the implementation of the DIYKit class.Programmers:TVB	Terry Van BelleAGR	Arthur RymanChange History:AGR	V100	1991/11/18	Upgraded to C++.AGR V101	1992/04/09	Comment out unused arguments.AGR	V102	1994/12/03	Minimize includes.AGR	V103	1995/08/04	Port to Symantec C++.AGR	V104	1995/08/06	Conditionally compile debugging code.AGR	V105	1995/12/23	Use OOP.AGR	V106	1995/12/25	Use NotationTool.AGR	V107	1995/12/27	Use WIND resource id.AGR	V108	1995/12/28	Set qualifiers after NewSign.AGR	V109	1995/12/28	Add UpdateSignQualifiers.AGR	V110	1995/12/28	Eliminate modeQualifiers.AGR	V111	1997/01/03	Add parent.*/#ifndef KIT_DIYKIT_H#include ":src:kit:diykit.h"	// DIYKit#endif#ifndef MAIN_MAIPROTO_H#include ":src:main:maiproto.h"	// Message#endif#ifndef MAIN_UTIL_H#include ":src:main:util.h"		// TRUE, FALSE#endif#ifndef MISC_NOTATOOL_H#include ":src:misc:notatool.h"	// NotationTool V106#endif#ifndef MISC_RESOURCE_H#include ":src:misc:resource.h"	// P_DIY#endif#ifndef MLINE_MLPROTO_H#include ":src:mline:mlproto.h"	// NewSpline#endif#ifndef MLINE_MLSPLINE_H#include ":src:mline:mlspline.h"	// Spline#endif#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// Sign#endif#ifndef SIGN_SIGPROTO_H#include ":src:sign:sigproto.h"	// NewSign#endif#define bThin		 0#define bMedium		 1#define bThick		 2#define bDotted		 3#define bCTDSpline	 4#define bLowCVSpline 5#define bMedCVSpline 6#define bHiCVSpline  7#define bLine		 8#define bArc		 9#define bCircPortion 10#define bWhite		 11#define bTransparent 12#define bBlack		 13#define bNone		127#define N_BUTTONS 	14#define N_SPLINES  	16#define N_LINES    	10#define N_CIRCLES  	 2#define N_SQUARES  	 2#define N_DIAMONDS 	 0#define N_TRIANGLES  0#define N_POLYGONS   2#define N_SIGNS		 N_SPLINES + N_LINES + N_CIRCLES + N_SQUARES + N_DIAMONDS +\                     N_TRIANGLES + N_POLYGONS#define firstSign	 N_BUTTONS#define TOT_SPLINES	168							/* Total number of elements in splines */#define TOT_POLYS	14int splines[] = { 16,  3,   0,  4,    4,  4,				  17,  3,   0, -4,   -4, -4,				  18,  3,   0, -4,    4, -4,				  19,  3,   0,  4,   -4,  4,				  20,  6,   0,  4,    0,  5,    2,  6,    4,  5,    4,  3,				  21,  6,   0, -4,    0, -5,    2, -6,    4, -5,    4, -3,				  22,  6,   0, -4,    0, -5,   -2, -6,   -4, -5,   -4, -3,				  23,  6,   0,  4,    0,  5,   -2,  6,   -4,  5,   -4,  3,				  24,  6,   0,  2,    0,  4,    4,  4,    4,  2,    4,  0,				  25,  6,  -2,  0,   -4,  0,   -4,  4,   -2,  4,    0,  4,				  26,  6,   2,  0,    4,  0,    4,  4,    2,  4,    0,  4,				  27,  6,   0, -2,    0, -4,    4, -4,    4, -2,    4,  0,				  28,  6,  -2, -2,   -4, -2,   -5, -1,   -5,  1,   -3,  3,				  29,  6,  -2,  2,   -2,  4,   -1,  5,    1,  5,    3,  3,				  30,  6,   2,  2,    2,  4,    1,  5,   -1,  5,   -3,  3,				  31,  6,   2, -2,    4, -2,    5, -1,    5,  1,    3,  3 };int splineptr = 0;int lines[] = {  6,  0, -4,				 7,  0, -2,				 8,  4,  0,				 9,  2,  0,				10,  4,  4,				11,  4, -4, 				12,  2,  2,				13,  2, -2,				14,  1,  5,				15,  1, -5 };int lineptr = 0;int circles[] = { 0,  3, 3,				  1,  7, 7 };int circleptr = 0;int squares[] = { 2,  3, 3,				  3,  7, 7 };int squareptr = 0;int diamonds[] = { -1 };int diamondptr = 0;int triangles[] = { -1 };int triangleptr = 0;int polygons[] = { 4,  4,   -3,  3,    0,  6,    3,   3,				   5,  3,   -3, -3,    3, -3 };int polygonptr = 0;static ItemInfo Items[] ={	/* 		Thickness buttons 	*/	special,  58,  30,  69,  45,	special,  58,  46,  69,  61,	special,  58,  62,  69,  76,		/* 		Dotted button		*/	special,  58,  81,  69,  96,		/* 		Modes buttons 		*/	special,  75,  17,  94,  37,  	special,  75,  41,  94,  61,	special,  75,  64,  94,  84,	special,  75,  88,  94, 108,	special,  99,  30, 119,  50,	special,  99,  54, 119,  73,	special,  99,  77, 119,  97,		/* 		colours buttons  	*/	special,   9,  84,  18,  93,	special,   9,  94,  18, 103,	special,   9, 104,  18, 113,	/*			SIGNS				*/	plainSign, 12, 18, 17, 23,				/* Circles */	plainSign, 11, 25, 17, 31,		plainSign, 12, 38, 17, 42,				/* Squares */	plainSign, 13, 43, 17, 49,		plainSign, 12, 57, 18, 65,				/* Misc. shapes */	plainSign,  9, 69, 18, 77,		plainSign, 22, 19, 30, 23,				/* Lines */	plainSign, 24, 25, 29, 29,	plainSign, 23, 35, 28, 42,	plainSign, 23, 44, 28, 50,	plainSign, 23, 54, 29, 60,	plainSign, 23, 63, 29, 69,	plainSign, 23, 75, 27, 79,	plainSign, 23, 82, 27, 86,	plainSign, 23, 93, 29, 97,	plainSign, 24, 99, 30, 103,		plainSign, 35, 14, 41, 20,				/* Splines */	plainSign, 36, 28, 41, 34,	plainSign, 36, 43, 41, 49,	plainSign, 35, 57, 41, 63,	plainSign, 34, 68, 41, 73,	plainSign, 35, 82, 41, 87,	plainSign, 35, 96, 41, 101,	plainSign, 34, 110, 41, 115,	plainSign, 47, 14, 52, 20,	plainSign, 46, 28, 52, 33,	plainSign, 46, 42, 52, 47,	plainSign, 47, 55, 53, 61,	plainSign, 46, 68, 52, 74,	plainSign, 46, 82, 52, 88,	plainSign, 46, 95, 52, 101,	plainSign, 46, 109, 52, 115};static Rect PictRect = {57, 16, 120, 109};KitInfo DIYKit::gInfo ={	W_DIYKit,	// V107	1,	sizeof(Items) / sizeof(ItemInfo),	Items};DIYKit* DIYKit::gDIYKit = 0;void DIYKit::Setup(Target* aParent)	// V111{	if(gDIYKit == 0)		gDIYKit = new DIYKit(aParent);}Kit* DIYKit::Create(Target* aParent)	// V111{	return new DIYKit(aParent);}DIYKit::DIYKit(Target* aParent) : 	Kit(aParent, gInfo),	// V111	numCVs(4),	dotted(FALSE),	colour(bWhite),	thickness(bMedium),	mode(bNone){	InvertKitItem(thickness);	InvertKitItem(colour);	if(gDIYKit)		DuplicatePrototype();	else		Initialize();}DIYKit::~DIYKit(){	if(mode != bNone)		NotationTool::ChangeCurrentMode(moveSymbolMode);	// V106}void DIYKit::Initialize(){	splineptr = lineptr = circleptr = squareptr = triangleptr = diamondptr = polygonptr = 0;	for(int i = 0; i < N_SIGNS; i++)	{		Sign** sign;		int npts;		int j;			if(splineptr < TOT_SPLINES && i == splines[splineptr])		{			npts = splines[++splineptr];			sign = NewSign (splineType);			SetQualifiers(sign);	// V108			Spline** spline = (**sign).u.spline = NewSpline(npts);						(**spline).npts = npts;			(**spline).pt[1].h = (**spline).pt[1].v = 0;			for(j = 2; j <= (**spline).npts; j++)			{				(**spline).pt[j].h = splines[++splineptr];				(**spline).pt[j].v = splines[++splineptr];			}			FixSpline(spline);			splineptr++;						SetKitItem(firstSign + i, sign);			continue;		}		if(lineptr < N_LINES * 3 && i == lines[lineptr])		{			sign = NewSign(lineType);			SetQualifiers(sign);	// V108			(**sign).u.delta.h = lines[++lineptr];			(**sign).u.delta.v = lines[++lineptr];			lineptr++;						SetKitItem(firstSign + i, sign);			continue;		}		if(circleptr < N_CIRCLES * 3 && i == circles[circleptr])		{			sign = NewSign(ovalType);			SetQualifiers(sign);	// V108			(**sign).u.box.colour = whiteFill;			(**sign).u.box.width = circles[++circleptr];			(**sign).u.box.height = circles[++circleptr];			circleptr++;						SetKitItem(firstSign + i, sign);			continue;		}		if(squareptr < N_SQUARES * 3 && i == squares[squareptr])		{			sign = NewSign (boxType);			SetQualifiers(sign);	// V108			(**sign).u.box.colour = whiteFill;			(**sign).u.box.width = squares[++squareptr];			(**sign).u.box.height = squares[++squareptr];			squareptr++;						SetKitItem(firstSign + i, sign);			continue;		}		if(i == diamonds[diamondptr])		{			continue;		}		if(i == triangles[triangleptr])		{			continue;		}		if(i == polygons[polygonptr])		{			sign = NewSign (polyType);			SetQualifiers(sign);	// V108			(**sign).u.poly = NewPolyStruct();						(**(**sign).u.poly).colour = whiteFill;						npts = polygons[++polygonptr];						PolyHandle poly = OpenPoly();				MoveTo(0, 0);				for(j = 2; j <= npts; j++)				{					int x = polygons[++polygonptr];					int y = polygons[++polygonptr];					LineTo (x, y);				}				LineTo (0, 0);			ClosePoly();						(**(**sign).u.poly).polygon = poly;			polygonptr++;						SetKitItem(firstSign + i, sign);			continue;		}				SetKitItem(firstSign + i, 0);	}}void DIYKit::DuplicatePrototype(){	for(int i = 0; i < info.nItems; i++)	{		if(gDIYKit->items[i].sign)			items[i].sign = DupSign(gDIYKit->items[i].sign);		else			items[i].sign = 0;		items[i].pos = gDIYKit->items[i].pos;		items[i].inverted = gDIYKit->items[i].inverted;	}}// V110#if 0void DIYKit::DoSettings(){	thickness = bMedium;	// V110 modeQualifiers.DIY.thickness = Medium;	InvertKitItem(bMedium);		colour = bWhite;	// V110 modeQualifiers.DIY.colour = (short) whiteFill;	InvertKitItem(bWhite);		numCVs = 4;	// V110	// V110 modeQualifiers.DIY.numCVs = 4;	mode = bNone;		dotted = FALSE;	// V110 modeQualifiers.DIY.dotted = dotted = FALSE;}#endifvoid DIYKit::Update(){	DrawPicture(GetPicture(P_DIY), &PictRect);		Rect r;	SetRect(&r, 83, 8, 114, 19);	FrameRect(&r);		MoveTo(93, 8);	Line(0, 10);	MoveTo(103, 8);	Line(0, 10);	MoveTo(83, 13);	Line(30, 0);		SetRect(&r, 86, 11, 91, 16);	FillOval(&r, &qd.white);		// V103	FrameOval(&r);		SetRect(&r, 96, 11, 101, 16);	FrameOval(&r);		SetRect(&r, 106, 11, 111, 16);	FillOval(&r, &qd.black);			// V103}void DIYKit::Event(int itemNo, Point, int){	int i;		if(itemNo < 0 || itemNo >= info.nItems)	{		Message1("Error in :kit:diykit.c: Event.  Item number out of range");	// V104		return;	}		if(itemNo >= firstSign)		return;			if(itemNo == thickness)		return;			if(itemNo == mode)	{		InvertKitItem(itemNo);		mode = bNone;		NotationTool::ChangeCurrentMode(moveSymbolMode);	// V106		return;	}		switch(itemNo)	{	case bDotted:											/* dotted */		InvertKitItem(bDotted);		// V110 dotted = modeQualifiers.DIY.dotted = !dotted;		dotted = !dotted;	// V110				for(i = firstSign; i < firstSign + N_SIGNS; i++)		{			KitItem* itemState = &items[i];			SetQualifiers(itemState->sign);			DrawKitItem(i);		}		return;			case bThick:											/* thickness */	case bMedium:	case bThin:		InvertKitItem(thickness);		InvertKitItem(itemNo);		thickness = itemNo;		// V110 modeQualifiers.DIY.thickness = (itemNo - bThin) + Thin;				for(i = firstSign; i < firstSign + N_SIGNS; i++)		{			KitItem* itemState = &items[i];			SetQualifiers(itemState->sign);			DrawKitItem(i);		}		return;			case bWhite:											/* colours */	case bBlack:	case bTransparent:		InvertKitItem(colour);		InvertKitItem(itemNo);		colour = itemNo;		// V110 modeQualifiers.DIY.colour = itemNo - bWhite;		// V110 FlipColours(modeQualifiers.DIY.colour);		FlipColours(colour - bWhite);	// V110		return;			case bCTDSpline:										/* modes */		NotationTool::ChangeCurrentMode(pCurveMode, this);	// V106		break;			case bLowCVSpline:		// V110 modeQualifiers.DIY.numCVs = 4;		numCVs = 4;		// V110		NotationTool::ChangeCurrentMode(sCurveMode, this);		break;			case bMedCVSpline:		// V110 modeQualifiers.DIY.numCVs = 7;		numCVs = 7;		// V110		NotationTool::ChangeCurrentMode(sCurveMode, this);		break;			case bHiCVSpline:		// V110 modeQualifiers.DIY.numCVs = 10;		numCVs = 10;	// V110		NotationTool::ChangeCurrentMode(sCurveMode, this);		break;			case bLine:		NotationTool::ChangeCurrentMode(lineMode, this);		break;			case bArc:		NotationTool::ChangeCurrentMode(arcMode, this);		break;			case bCircPortion:		break;			default:		Message1("Error: Invalid Mode in :kit:diykit.c");	// V104	}	/* If we have reached this point, we are processing a mode, so change it */		if(mode != bNone) 		InvertKitItem(mode);	mode = itemNo;	InvertKitItem(itemNo);}void DIYKit::FlipColours(int newColour){	int circleptr, squareptr, itemNo;	Sign **sign;		for(circleptr = 0; circleptr < N_CIRCLES * 3; circleptr += 3)	{		itemNo = circles[circleptr] + firstSign;		sign = items[itemNo].sign;		if(newColour == transparent)		{			(**sign).u.box.colour = whiteFill;			SetKitItem(itemNo, sign);			DrawKitItem(itemNo);		}		(**sign).u.box.colour = newColour;		SetKitItem(itemNo, sign);		if(newColour != transparent)			DrawKitItem(itemNo);	}	for(squareptr = 0; squareptr < N_SQUARES * 3; squareptr += 3)	{		itemNo = squares[squareptr] + firstSign;		sign = items[itemNo].sign;		if(newColour == transparent)		{			(**sign).u.box.colour = whiteFill;			SetKitItem(itemNo, sign);			DrawKitItem(itemNo);		}		(**sign).u.box.colour = newColour;		SetKitItem(itemNo, sign);		if(newColour != transparent)			DrawKitItem(itemNo);	}	for(polygonptr = 0; polygonptr < TOT_POLYS;		 polygonptr += 2 * polygons[polygonptr + 1])	{		itemNo = polygons[polygonptr] + firstSign;		sign = items[itemNo].sign;		if(newColour == transparent)		{			(**(**sign).u.poly).colour = whiteFill;			SetKitItem(itemNo, sign);			DrawKitItem(itemNo);		} 		(**(**sign).u.poly).colour = newColour;		SetKitItem(itemNo, sign);		if(newColour != transparent)			DrawKitItem(itemNo);	}}void DIYKit::Reset(){	if(mode == bNone)		return;			InvertKitItem(mode);		mode = bNone;}Boolean DIYKit::UpdateSignQualifiers(Sign** sign)	// V109{	Boolean bUpdated = FALSE;	switch((**sign).type)	{	case splineType:	case boxType:	case ovalType:	case polyType:	case lineType:	case arcType:		(**sign).qual.DIY.dashed = dotted;		(**sign).qual.DIY.thickness = (thickness - bThin) + Thin;		bUpdated = TRUE;	}		return bUpdated;}// Virtual DIYKit functions. V110short DIYKit::Thickness(){	short result = (thickness - bThin) + Thin;		return result;}Boolean DIYKit::Dotted(){	Boolean result = dotted;		return result;}short DIYKit::NumCVs(){	short result = numCVs;		return result;}short DIYKit::Colour(){	short result = colour - bWhite;		return result;}