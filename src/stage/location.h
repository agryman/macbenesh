/* * Version 3 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * location.h * * written by Jennifer Hyndman * * Change History: * 1991/11/27 AGR V100	Upgraded to C++.AGR	V101	1994/11/20	Replace struct by class. */ #ifndef STAGE_LOCATION_H#define STAGE_LOCATION_H/* Generate debugging messages */#undef STAGE_DEBUGclass Tick{public:	char across;	char upstage;};class LocationLine{public:	/* how many ticks are associated with this line? */	char nticks;	/* the tick locations with respect to the line origin */	Tick **ticks;	/* the distance from this line origin to the next on the	   right line.  The origin is the base of the symbol	*/	char distance;};/* Information used in a stage sign. */class LocateData{public:	/* Is the whole location symbol dotted? */	Boolean dotted;	/* The number of individual location lines making up this sign */	char howmany;	/* The bounding box of the location sign. */	Rect bounds;	/* The information for each location line */	LocationLine **location[9];};class StageLine{public:	/* which horizontal line location is this line? */	char line;	/* tick position across the stage with respect to this line */	char across;	/* upstage/downstage positon */	char upstage;	/* pointer to the next line */	StageLine **next;};class StageData{public:	Boolean dotted;	StageLine **places;};#endif