/*	name:	toolbox:window.c	type:	C++		This file defines functions for use with windows.Programmers:AGR	Arthur RymanChange History:AGR	V100	1994/08/17	Created.*/#ifndef HEADER_H#include ":src:header.h"			// WindowPtr, etc.#endif#ifndef MAIN_UTIL_H#include ":src:main:util.h"		// FALSE#endif#ifndef MAIN_WINDOWS_H#include ":src:main:windows.h"		// WindowWidth, WindowHeight#endif#ifndef TOOLBOX_WINDOW_H#include ":src:toolbox:window.h"	// CentreWindow#endif/* Centre theWindow in the screen. */void CentreWindow(WindowPtr theWindow){	/* Center the window on the screen. */		Rect bounds = qd.screenBits.bounds;		short left = (bounds.left + bounds.right - WindowWidth(theWindow)) / 2;	short top = (bounds.top + bounds.bottom - WindowHeight(theWindow)) / 2;	MoveWindow(theWindow, left, top, FALSE);}