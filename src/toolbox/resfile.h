/*	name:	resfile.h	type:	C++		A ResourceFile object is a proxy for a MacOS resource file.Programmers:AGR	Arthur RymanChange History:AGR	V100	1997/03/26	Created.*/#ifndef TOOLBOX_RESFILE_H#define TOOLBOX_RESFILE_Hclass ResourceFile{public:	ResourceFile (short file);	OSErr read (ResType resType, short resID, Handle* pHandle);	OSErr write (ResType resType, short resID, Str255 resName, Handle handle);private:	short resFile;};#endif