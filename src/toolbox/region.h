/*	name:	:toolbox:region.h	type:	C++		This is the class declaration for TRegion which encapsulates the MacintoshToolbox support for the Region data structure. The benefits of using this classare that		1) Local TRegion objects are automatically disposed when they go out of scope.		This reduces coding, is less error prone, and is compatible with		throwing exceptions.		2) A natural mathematical syntax can be used for region calculations. We		use *, +, -, and ^ for intersection, union, difference, and xor. The		first three of these operators are used for the same set operators in		Pascal and Modula.		3) The programmer doesn't have to remember the order of parameters to		the toolbox functions since most are replaced by operators.			4) A variety of useful constructors are provided which again reduces the		amount of code that the programmer has to write.		Programmers:AGR	Arthur RymanChange History:AGR	V100	1993/12/08	Created.*/#ifndef TOOLBOX_REGION_H#define TOOLBOX_REGION_Hclass TRegion{	RgnHandle rgn;public:	TRegion(void);	TRegion(const TRegion& srcRegion);	TRegion(const RgnHandle srcRgn);	TRegion(const Rect& srcRect);	TRegion(short left, short top, short right, short bottom);	~TRegion(void);	static void Open(void);	TRegion& Close(void);	TRegion& operator=(const TRegion& srcRegion);	TRegion& operator=(const RgnHandle srcRgn);	TRegion& operator=(const Rect& srcRect);	TRegion& SetEmpty(void);	TRegion& SetRect(short left, short top, short right, short bottom);	TRegion& Offset(short dh, short dv);	TRegion& Inset(short dh, short dv);	TRegion operator*(const TRegion& rightRegion) const;	TRegion operator+(const TRegion& rightRegion) const;	TRegion operator-(const TRegion& rightRegion) const;	TRegion operator^(const TRegion& rightRegion) const;	TRegion& operator*=(const TRegion& rightRegion);	TRegion& operator+=(const TRegion& rightRegion);	TRegion& operator-=(const TRegion& rightRegion);	TRegion& operator^=(const TRegion& rightRegion);	Boolean PtIn(const Point& pt) const;	Boolean RectIn(const Rect& r) const;	Boolean operator==(const TRegion& rightRegion) const;	Boolean Empty(void) const;	TRegion& Frame(void);	TRegion& Paint(void);	TRegion& Erase(void);	TRegion& Invert(void);	TRegion& Fill(ConstPatternParam pat);	TRegion& Inval(void);	TRegion& Valid(void);	TRegion& Map(const Rect& srcRect, const Rect& dstRect);	TRegion& GetClip(void);	TRegion& SetClip(void);	TRegion& ScrollRect(const Rect& r, short dh, short dv);};#endif