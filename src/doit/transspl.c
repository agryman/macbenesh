/* * Version 2 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * Spline transformations. * * Change History: * 1991/11/17	AGR	V100	Upgrade to C++.AGR	V101	1994/12/05	Minimize includes.AGR	V102	1995/08/06	Conditionally compile debugging code.AGR	V103	1996/01/02	Use OOP for Score.AGR	V104	1997/03/14	Eliminate globals.*/#ifndef HEADER_H#include ":src:header.h"		// Point#endif#ifndef DOIT_DOIT_H#include ":src:doit:doit.h"		// T_Mirror#endif#ifndef DOIT_DIYPROTO_H#include ":src:doit:diyproto.h"	// TransformSpline#endif#ifndef MAIN_MAIPROTO_H#include ":src:main:maiproto.h"	// Message#endif#ifndef MLINE_MLPROTO_H#include ":src:mline:mlproto.h"	// SortSpline#endif#ifndef MLINE_MLSPLINE_H#include ":src:mline:mlspline.h"	// Spline#endif#ifndef MISC_SCALE_H#include ":src:misc:scale.h"	// ScoreScale#endif#ifndef MISC_SCORES_H#include ":src:misc:scores.h"	// Score#endif#ifndef SIGN_SIGPROTO_H#include ":src:sign:sigproto.h"	// DupSign#endif#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// Sign#endifSign **TransformSpline(Sign **sign, Point *pos, Rect *boundsRect,						char transformation, Score* score)	// V104{	Sign **newSign = DupSign(sign);	Spline **spline = (**newSign).u.spline;	int scale = score->ScoreScale();		switch (transformation) {		case T_Mirror:		case T_FlipHoriz:			FlipCVHoriz (spline);			break;		case T_FlipVert:			FlipCVVert (spline);			break;		case T_Rotate:			RotateSpline (spline);			break;		default:			Message1("transformSpline: Unknown transformation");	// V102			return 0;	}	AlterPos (pos, boundsRect, transformation, score);	// V104	SortSpline (newSign, pos, scale);		return newSign;}void FlipCVHoriz(Spline **spline){	short i;		for (i = 1; i <= (**spline).npts; i++)		(**spline).pt[i].h *= -1;}void FlipCVVert(Spline **spline){	short i;		for (i = 1; i <= (**spline).npts; i++)		(**spline).pt[i].v *= -1; }void RotateSpline(Spline **spline){	int i, temp;		for (i = 1; i <= (**spline).npts; i++) {		temp = (**spline).pt[i].h;		(**spline).pt[i].h = -(**spline).pt[i].v;		(**spline).pt[i].v = temp;	}}				