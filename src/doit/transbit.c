/* * Version 2 of the Macintosh Editor for Benesh Movement Notation * Copyright 1984 by Doug Moen * * Type-independant sign transformations by Terry Van Belle. * * Change History: * 1991/11/17	AGR	V100	Upgrade to C++. * 1992/04/09	AGR V101	Use C style type casts for C compatibility.AGR	V102	1994/12/05	Minimize includes.AGR	V103	1996/01/02	Use OOP for Score.AGR	V104	1997/03/14	Eliminate globals.*/#ifndef HEADER_H#include ":src:header.h"		// Point#endif#ifndef DOIT_DOIT_H#include ":src:doit:doit.h"		// T_Mirror#endif#ifndef DOIT_DIYPROTO_H#include ":src:doit:diyproto.h"	// TransformBitMap#endif#ifndef MISC_SCALE_H#include ":src:misc:scale.h"	// ScoreScale#endif#ifndef MISC_SCORES_H#include ":src:misc:scores.h"	// Score#endif#ifndef SIGN_SIGPROTO_H#include ":src:sign:sigproto.h"	// BoundsSign#endifSign** TransformBitMap(Sign **sign, Point *pos, Rect *boundsRect,						char transformation, Score* score)		// V104{	int scale = score->ScoreScale();	Rect r;		AlterPos (pos, boundsRect, (int) transformation, score);	// V101, V104	BoundsSign (sign, &r);		switch (transformation)	{	case T_Mirror:		pos->h += scale;	case T_FlipHoriz:		pos->h -= r.left + r.right;		break;			case T_FlipVert:		pos->v -= r.top + r.bottom;		break;			case T_Rotate:		break;	}		return 0;}			