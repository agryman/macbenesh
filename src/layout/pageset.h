/*	name:	:layout:pageset.h	type:	MPW C++ 3.2		This is the interface to the TPageSetup class.Programmers:AGR	Arthur RymanChange History:AGR	V100	1992/08/15	Created.AGR	V101	1992/09/17	Added file i/o.AGR	V102	1993/12/04	Guard against repeated inclusion.AGR	V103	1995/08/04	Port to Symantec C++.*/#ifndef LAYOUT_PAGESET_H#define LAYOUT_PAGESET_H#include <Printing.h>	// V103 TPrintclass TPageSetup{	Rect paper;						// paper rectangle in pts	Rect page;						// page rectange in pts, top left = (0, 0)	TPrint prRec;					// possibly invalid print recordpublic:	TPageSetup(void);				// initialize paper and page rectangles to defaults	Rect Paper(void) {return paper;}	Rect Page(void) {return page;}	void GetPrintRecord(THPrint);	// get current prRec	void SetPrintRecord(THPrint);	// set prRec and update paper and page rectangles	void DumpPrintRecord(void);		// display key contents of prRec	int Read(short dataFile, short resFile);				// V101	int Write(short dataFile, short resFile);				// V101};#endif