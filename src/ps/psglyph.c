/**	psglyph.c**	This file contains the PostScript Glyph package.** Change History** 1990/01/06 AGR V100	Completed file.* 1991/11/27 AGR V200	Upgraded to C++.AGR	V201	1994/11/26	Minimize includes.*/#ifndef HEADER_H#include ":src:header.h"		// Point#endif#ifndef MAIN_ASSERT_H#include ":src:main:assert.h"	// assert#endif#ifndef PS_PSPROTO_H#include ":src:ps:psproto.h"		// PSGlyph#endif#ifndef SIGN_SIGPROTO_H#include ":src:sign:sigproto.h"	// GlyphBounds#endif#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// Sign#endifvoid PSGlyph(Sign *s, int font, int glyph, Point pos){	Rect bounds;		GlyphBounds(font, glyph, &bounds);	OffsetRect(&bounds, pos.h, pos.v);		switch(s->type)	{	case DigitType:		PSDigit(pos, bounds, s->u.which);		break;			case direction:		PSDirection(pos, bounds, s->u.which);		break;			case torsoType:		PSTorso(pos, bounds, 			s->u.torso.tilt, s->u.torso.bend, s->u.torso.turn);		break;			case rhythmType:		PSRhythm(pos, bounds, s->u.rhythm.which, s->u.rhythm.grace);		break;			case limbType:		PSLimb(pos, bounds, s->u.limb.which,			s->u.limb.lateralCrossover, s->u.limb.verticalCrossover);		break;			case contactType:		PSContact(pos, bounds, s->u.which);		break;			default:		assert(false);		break;	}}