/**	psbrack.c**	This file contains the PostScript Bracket package.** Change History** 1990/01/06 AGR V100	Completed file.* 1991/11/24 AGR V200	Upgraded to C++.AGR	V201	1994/11/26	Minimize includes.*/#ifndef MAIN_ASSERT_H#include ":src:main:assert.h"	// assert#endif#ifndef PS_PSPROTO_H#include ":src:ps:psproto.h"		// PSBracket#endif#ifndef SIGN_SIGNS_H#include ":src:sign:signs.h"	// brackLeft#endif#define BracketLength		mm(5.0)#define BracketHeight		mm(2.0)#define BracketLineWidth	mm(0.25)void PSBracket(short which, Point pos){	float x1, y1, x2, y2, x3, y3, x4, y4;		assert(which == brackLeft || which == brackRight);		x3 = pos.h + 0.5;	y3 = pos.v - 2.5;		x1 = x3;	y1 = y3 + BracketHeight / 2.0;		x2 = x3;	y2 = y3 - BracketHeight / 2.0;		x4 = x3 + (which == brackLeft ? BracketLength : -BracketLength);	y4 = y3;		PS_moveto(x1, y1);	PS_lineto(x2, y2);		PS_moveto(x3, y3);	PS_lineto(x4, y4);		PS_setlinewidth(BracketLineWidth);	PS_stroke();}